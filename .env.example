# Copie ce fichier en .env puis remplis un mot de passe local.
# .env (le vrai) est ignoré par Git — un mot de passe, même "juste pour du
# dev local", n'a rien à faire dans l'historique d'un dépôt versionné.

PGDATABASE=appdb
PGUSER=appuser
PGPASSWORD=change-me-local-dev-only
